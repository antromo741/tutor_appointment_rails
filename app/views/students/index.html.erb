<h1>Students List</h1>
<button><%= link_to  "Register Student", new_student_path %></button>
<% @students.each do |student| %>

    <p><%= link_to student.name, student_path(student) %></p>

<% end %>