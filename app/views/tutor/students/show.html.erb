<h1><%= @student.name %></h1>
<p><%= link_to  "Book appointment with #{@student.name}", root_path(@student) %></p>
<p><%= link_to  "#{@student.name}'s appointments", student_appointments_path(@student) %></p>

</p>